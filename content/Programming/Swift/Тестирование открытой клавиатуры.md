---
title: Тестирование открытой клавиатуры
draft: false
tags:
  - develop
  - testing
  - UI
---
Для тестирования клавиатуры необходимо добавить экран на UIWindow в тестах

```swift
func test_textFieldEndsEditing_onViewModelKeyboardEnabled() {
	let (sut, vm) = makeSUT()
	let window = UIWindow()
	window.addSubview(sut)

	vm.isKeyboardEnabled.onNext(false)
	XCTAssertFalse(sut.inputTextField.isEditing)
}
```